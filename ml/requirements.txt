requests>=2.31
Pillow>=10.0

# train_model.py precisa de tensorflow. Treino roda 100% local em CPU (ver
# docstring de scripts/train_model.py) -- sem GPU no Windows, nem NVIDIA sem WSL2
# nem AMD (o plugin DirectML foi descontinuado).
tensorflow>=2.20
